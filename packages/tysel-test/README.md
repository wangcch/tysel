# @tysel/test

Test helpers for applications running under `tysel test`.

```ts
import { assert, test } from "@tysel/test";

test("example", () => {
  assert.equal(1 + 1, 2);
});
```

The package version must match the installed Tysel native toolchain.
