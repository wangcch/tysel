---
title: "fetch"
description: "Tysel JavaScript 运行时支持的 fetch 约定。"
---

状态：`partial`。本页描述受支持的服务端子集，而非完整的浏览器规范。
下列清单以外的行为不属于兼容性约定。

事实来源：`runtime-js/web-api/compatibility.json`
（稳定的 `tysel-server-web-subset`）。

## 支持

- 宿主策略允许的 HTTP 方法
- 请求头和缓冲的请求正文
- 重定向策略
- 以流方式消费响应
- 通过 AbortSignal 取消请求

## 不支持

- 流式请求正文
- multipart/form data
- 浏览器的缓存、凭据和 mode 语义

另请参阅 [JavaScript API 索引](/reference/javascript)和
[npm 兼容性指南](/docs/compatibility)。
