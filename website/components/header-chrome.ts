/** Shared hit target + glyph weight for header utility controls. */
export const headerIconButtonClass =
  "inline-flex size-8 shrink-0 cursor-pointer items-center justify-center text-fd-muted-foreground transition-colors hover:bg-fd-accent hover:text-fd-foreground";

/** Lucide / SVG glyph size inside the hit target. */
export const headerIconClass = "size-4";

/** Short locale labels (EN / 中) sized to match icon optical weight. */
export const headerIconLabelClass = "font-mono text-sm leading-none font-medium";
